# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/school_results
DATABASE_SSL=false

# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=School Results <no-reply@example.com>

# App Configuration
NEXT_PUBLIC_APP_NAME=School Results
